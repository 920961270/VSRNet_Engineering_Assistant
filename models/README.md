# Models Folder

Stage 1 keeps model folders in the project layout, but the default embedding model path points to the already downloaded local model:

```text
G:\4周学习计划\第三周\VSRNet_RAG_Demo\models\all-MiniLM-L6-v2
```

Real VSRNet weights and local embedding model copies can be placed here later if desired.

